# Git команды
## Присоединить уже существующий
`git init`

`git remote add origin`

`git config --global user.name "TAHA8881"`

`git config --global user.email "shaprina.tatiana888@gmail.com"`

`git status`

`git init`

`git remote add origin https://github.com/TAHA8881/praktika_TAHA.git`

`git remote -v`

`git add .`

`git pull origin main --allow-unrelated-histories`

`git push -u origin main`

`cd praktika_TAHA`

`git add .`

`git commit -m "проверка гита11"`

`git push`

## Если отвалится

`cd praktika_TAHA`
`git add .`
`git commit -m "проверка гита888"`
`git push`

## Чтобы обновить всё
`git pull`

## Чтобы скопировать всё

`git clone https://github.com/TAHA8881/praktika_TAHA.git`

`git clone https://github.com/TAHA8881/bogdanov_practica_09_06_2026`

`git clone https://github.com/aisuluu56/python-learning`

`git clone https://gitverse.ru/elrdn/python_syntax_practice.git`

## Дополнительно

`Markdown Preview Enhanced`

`https://disk.yandex.ru/d/zBwXvpJMJcFnYA`

Пароль от гита:
`1771117Nat+`

какой то psycopg2
`pip install psycopg2-binary`

Советовал учитель:
`git stash`
`git pull`
`git stash pop`






